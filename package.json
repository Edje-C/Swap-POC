{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon ./bin/www",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "body-parser": "*",
    "cookie-parser": "*",
    "debug": "*",
    "dotenv": "*",
    "express": "*",
    "express-session": "*",
    "jade": "*",
    "morgan": "*",
    "passport": "*",
    "passport-spotify": "*",
    "pg-promise": "*",
    "querystring": "*",
    "request": "*",
    "serve-favicon": "*"
  },
  "devDependencies": {
    "nodemon": "*"
  }
}
